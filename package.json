{
  "name": "sandcastling",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@10.31.0",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "sandcastle": "tsx .sandcastle/main.ts",
    "build:image": "docker build -t sandcastle:latest .sandcastle",
    "clean": "docker volume rm -f sandcastle-node-modules sandcastle-pnpm-store",
    "typecheck": "tsc --noEmit",
    "lint": "biome lint .",
    "lint:fix": "biome lint --write .",
    "format": "biome format --write .",
    "format:check": "biome format .",
    "check": "biome check .",
    "check:fix": "biome check --write .",
    "test": "node --import tsx --test .sandcastle/lib/tests/*.test.ts .sandcastle/sandboxes/*/tests/*.test.ts",
    "verify": "pnpm typecheck && pnpm check && pnpm test"
  },
  "dependencies": {
    "chalk": "^5.3.0",
    "ulid": "^3.0.2"
  },
  "devDependencies": {
    "@ai-hero/sandcastle": "latest",
    "@biomejs/biome": "^1.9.4",
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  }
}
